# nixpacks.toml

[phases.setup]
# 1. We include 'pnpm' here so it is installed at the system level.
# 2. We add 'jq' and 'wget' to FORCE a cache refresh (Cache Busting).
nixPkgs = ["nodejs-20_x", "openssl", "pnpm", "jq", "wget"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pnpm install"]

[phases.build]
dependsOn = ["install"]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
```

### **Critical Step: Push to Git**
Since Dokploy pulls from your repository, you **must commit and push** this file to GitHub/GitLab for the changes to take effect on your VPS.
